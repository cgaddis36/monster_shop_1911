
  <h1>All Items</h1>
<section class="grid-container">
  <% @items.each do |item| %>
  <% if item.active? %>
    <section class = "grid-item" id= 'item-<%=item.id%>'>
      <h2> <%=link_to item.name, "/items/#{item.id}" %> </h2>
      <p>Sold by: <%=link_to item.merchant.name, "/merchants/#{item.merchant.id}" %></p>
      <%= link_to image_tag(item.image), "items/#{item.id}" %>
      <p> <%= item.description unless @merchant%> </p>
      <p>Price: <%=number_to_currency(item.price) %> </p>
      <p>Inventory: <%= item.inventory %> </p>
    </section>
    <% end %>
  <% end %>
</section>

<h1> the most popular items are : <% Item.most_popular_items.each do |item| %> </h1>
                                 <p><%= item.name %>, <%= item.qty %> </p><br>
                                 <% end %>


<h1> the least popular items are : <% Item.least_popular_items.each do |item|%> </h1>
                                  <p><%= item.name %>, <%= item.qty %> </p><br>
                                  <% end %>
